import { StyleSheet } from 'react-native';
import { HEIGHT_SEARCH_ICON, WIDTH_SEARCH_ICON } from '@constants/sizes';

const styles = StyleSheet.create({
  image: {
    height: HEIGHT_SEARCH_ICON,
    width: WIDTH_SEARCH_ICON
  }
});

export default styles;
